#pragma once

#include "SFML/Graphics.hpp"

const unsigned POINTSCOUNT_DEFAULT = 1920;
const sf::Vector2f CENTER_DEFAULT(960.f, 540.f);
const sf::Vector2f SCALER_DEFAULT(1.f, 600.f);
const sf::Vector2f INTERVAL_DEFAULT(-100.f, 100.f);
const sf::Vector2f INTERVAL_INVALID(1.f, -1.f);